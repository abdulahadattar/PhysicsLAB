storage
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read; // Allows public read access to all files
      allow write: if request.auth != null && request.auth.token.role == 'teacher';
    }
  }
}